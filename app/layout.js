import './globals.css';

export const metadata = { title: 'Data Analyst Copilot', description: 'Ask your data questions in natural language.' };

export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html>; }
